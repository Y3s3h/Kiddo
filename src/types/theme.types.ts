export interface Theme {
  primary: string;
  background: string;
}

export interface Campaign {
  name: string;
}

// export interface Overlay {
//   type: "FULL_SCREEN_OVERLAY";
//   animationUrl: string;
// }